<section class="container justify-center">
  <h1 class="text-4xl font-bold leading-normal mt-0 text-black-800">
    Weather
  </h1>
  <h2 class="text-xl font-normal leading-normal mt-0 mb-2 text-pink-800"><%= Date.today.strftime('%A, %B %d') %></h2>
  <p>Today it is <%= @weather.status %></p><img src="<%= @weather.icon_url %>" />
  <p>There seems to be <b><%= @weather.description %></b>.</p>
  <p>Temperature is <%= @weather.temperature %></p>
</section>